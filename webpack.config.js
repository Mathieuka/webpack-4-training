module.exports = {
    // defining entry point of the bundle
    entry:"./src/index.js",
    // defining the output point of the bundle
    output: {
        filename: "bundle.js",
        path: "./dist"
    },
    mode: "none"
}